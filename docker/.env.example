# Copie para .env na mesma pasta e ajuste se precisar:
#   copy .env.example .env   (PowerShell/cmd no diretório docker/)
#   cp .env.example .env     (Git Bash / WSL)

POSTGRES_DB=fullstack_db
POSTGRES_USER=fullstack_user
POSTGRES_PASSWORD=change_me
